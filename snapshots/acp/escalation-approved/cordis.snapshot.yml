# Replay-only patch layered after `cordis.yml`: replace the network adapter
# with recorded JSONL, pin the recorded top-level model, and keep persistence
# raw for fixture harvesting.

- id: llm-deepseek
  name: '@singula-ai/alego-llm-deepseek'
  disabled: true

- id: acp
  name: '@singula-ai/alego-acp'
  config:
    provider: deepseek-official
    model: deepseek-v4-flash

- id: session-persistence-jsonl
  name: '@singula-ai/alego-session-persistence-jsonl'
  config:
    root: !!js process.env.ALEGO_SNAPSHOT_SESSIONS_ROOT ?? alegoHomePath('sessions')
    compression: none

- id: sandbox
  name: '@singula-ai/alego-sandbox-local'
  config:
    runnerCommand:
      - bash
      - -c
      - while [ "$1" != "--" ]; do shift; done; shift; exec "$@"
      - passthrough-runner
    runnerFailureSignatures:
      - 'passthrough-runner: profile rejected'

- insert:
    - id: llm-replay
      name: '@singula-ai/alego-llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
              - id: deepseek-v4-pro

    - id: subagent-settlement-marker
      name: './subagent-settlement-marker.ts'

- insert:
    - id: snapshot-shell-path
      name: ../../../scripts/snapshot-shell-path.ts
      config:
        command: "printf 'escalated\\n' > /tmp/alego-escalated.txt && cat /tmp/alego-escalated.txt && rm /tmp/alego-escalated.txt"
        recordedPath: /tmp/alego-escalated.txt
        livePath: !!js process.env.ALEGO_SNAPSHOT_SPILL_ROOT + '/alego-escalated.txt'
