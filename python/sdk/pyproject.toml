[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "alego-sdk"
version = "0.0.0.dev0"
description = "Python SDK for Alego"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "DeepSeek" }]
dependencies = [
  "pydantic>=2.12,<3",
  "alego-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/singula-ai/alego"
Documentation = "https://github.com/singula-ai/alego/blob/main/docs/user/guide/python-sdk.md"
Issues = "https://github.com/singula-ai/alego/issues"
Source = "https://github.com/singula-ai/alego"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/alego"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
alego-runtime-bin = { path = "../sdk-runtime", editable = true }
