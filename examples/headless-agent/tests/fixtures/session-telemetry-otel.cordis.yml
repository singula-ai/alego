# Test-only composition: session-telemetry-otel through the real Loader/app
# path, exporting to the mock OTLP collector the driver starts (url via env).
# The redact-rule entry models a deployment mounting its own scrub rule on the
# session-telemetry/record waterfall — the seam itself ships no rules.
- id: logger-console
  name: '@singula-ai/cordis-plugin-logger-console'
  config:
    colors: false
    levels:
      default: 3
    showTime: ''

- id: cli-mock-llm
  name: './cli-mock-llm.ts'

- id: telemetry-redact-rule
  name: './telemetry-redact-rule.ts'

# Managed child-process groups required by the bash executor.
- id: subprocess
  name: '@singula-ai/alego-subprocess-local'

- id: bash
  name: '@singula-ai/alego-bash-local'

- id: session-telemetry-otel
  name: '@singula-ai/alego-session-telemetry-otel'
  config:
    mode: !!js process.env.ALEGO_TELEMETRY_E2E_MODE || 'FULL'
    exporter:
      url: !!js process.env.ALEGO_TELEMETRY_E2E_URL

- id: agent-spine
  name: '@singula-ai/alego-agent-spine-demo'
  config:
    agents:
      - id: main
        provider: cli-mock
        model: cli-mock
        cwd: !!js process.cwd()
    persona: 'Test the session-telemetry-otel plugin.'
    workspaceContext: false

- id: persistence
  name: '@singula-ai/alego-session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: 'none'

- id: checkpoint-policy
  name: '@singula-ai/alego-session-checkpoint-policy'
